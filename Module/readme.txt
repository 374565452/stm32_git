
1：此文件夹下存放一些常用的模块驱动程序、应用模块、公用模块函数等
   
   a)矩阵式按键扫描算法实现、按键处理程序
   
   b)公用模块函数：无符号校验和算法、利用系统自带的printf函数实现串口数据的格式化打印操作
   
   c)带有按键与LCD屏一起联动的业务系统中，相关的数据结构与相应屏幕动作查找算法。（此处查找是按照数组查找的方式来进行的）同时菜单动作的索引号与一同
     配置在相应的菜单menu结构体对象中，具体的调用，是在相应的按键处理函数中调用。
	 在m_menu结构体中有指向父菜单的指针与指向所有孩子菜单的指针，这样就可以将所有的菜单联系起来，在系统初始时，定义一个current_menu指向开机后
	 显示的首屏，做为所有屏的老祖宗，其他的屏都是这个屏的孩子、孙子或孙子的。。。。
	 每一次按键操作后，其current_menu都需要进行变化，current_menu的值始终指向当前显示的屏
   d)